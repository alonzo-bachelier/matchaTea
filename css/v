body { 
    background-color: #f7f7f7; /* Couleur de fond plus claire */
    font-family: Arial, sans-serif; /* Police de caractères par défaut */
}

form {
    background-color: #ffffff; /* Couleur de fond du formulaire */
    border-radius: 10px;
    padding: 30px; /* Ajout de padding pour l'espace intérieur */
    margin-top: 50px;
    box-shadow: 0px 0px 20px rgba(0, 0, 0, 0.1); /* Légère ombre pour le formulaire */
}

form h2 {
    font-size: 28px;
    font-family: 'Pacifico', cursive; /* Police Pacifico pour le titre */
    color: #333333; /* Couleur du texte du titre */
    margin-bottom: 20px;
}

.corpsFormulaire {
    display: flex;
    flex-wrap: wrap; /* Permet aux éléments de passer à la ligne si nécessaire */
    gap: 20px;
    background-color: #f9f9f9; /* Couleur de fond légèrement plus claire */
    border: 1px solid #cccccc; /* Bordure plus légère */
    border-radius: 8px;
    padding: 20px;
}

.piedFormulaire {
    display: flex;
    justify-content: center;
    background-color: #f9f9f9;
    padding-top: 10px;
    padding-bottom: 10px;
}

.gauche, .droite {
    flex: 1;
}

.boite {
    padding-top: 10px;
}

/* STYLE DES CASES DU FORMULAIRE */

.input, .inputMessage {
    width: 100%;
    height: 45px;
    padding: 12px;
    border: 1px solid #999999; /* Bordure plus foncée */
    border-radius: 5px;
    outline: none;
    transition: border-color 0.3s ease;
}

.inputMessage {
    height: 150px;
}

.input:hover, .input:focus, .inputMessage:hover, .inputMessage:focus {
    border-color: #ff7f50; /* Couleur de la bordure au survol/focus */
}

/* Couleurs vives pour les icônes */
.fa-solid, .fa-regular {
    color: #ff7f50;
}
